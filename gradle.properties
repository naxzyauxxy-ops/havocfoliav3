group = gg.havoc

# This is the current API version for use in (paper-)plugin.yml files
# During snapshot cycles this should be the anticipated version of the release target
apiVersion = 26.2
mcVersion = 26.2
channel = STABLE

# Pinned by the CraftCanvasMC/Baguette template, which is the base this repo was
# generated from. The committed build.gradle.kts.patch files are anchored to THIS
# commit, so change it only together with a `rebuildCanvasSingleFilePatches` run.
canvasCommit = 27c56222f225d98b71f1739c0ca03d1b0ccd4b09

org.gradle.configuration-cache = true
org.gradle.caching = true
org.gradle.parallel = true
org.gradle.vfs.watch = false
org.gradle.jvmargs = -Xmx4G
